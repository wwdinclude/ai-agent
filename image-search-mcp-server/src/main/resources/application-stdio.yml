spring:
  ai:
    mcp:
      server:
        name: image-search-mcp-server
        version: 0.0.1
        type: SYNC
        # stdio
        stdio: true
  # 关闭 web 支持
  main:
    web-application-type: none
    banner-mode: off